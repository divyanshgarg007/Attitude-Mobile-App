import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    tabBar: {
        flexDirection: 'row',
    },
    tabItem: {
        flex: 1,
        flexDirection: 'row',
        //alignItems: 'center',
        //paddingHorizontal: 10,

    },
});